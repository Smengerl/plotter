# pipeline/requirements.txt
# Pinned dependency versions for reproducible environments.
#
# Python-Version: 3.13 (empfohlen) — vpype 1.15.x erfordert Python >=3.11,<3.14
#                                     Python 3.14 wird von vpype noch NICHT unterstützt.
#
# Schnellstart:
#   ./setup_pipeline.sh
#
# Manuell:
#   python3.13 -m venv .venv
#   source .venv/bin/activate
#   pip install -r pipeline/requirements.txt
#
# GPU-Beschleunigung (PyTorch):
#   Ersetze "torch" durch den passenden CUDA/ROCm-Build von https://pytorch.org
#
# ============================================================
# 1. KERN — immer benötigt
# ============================================================

numpy>=1.25,<3
opencv-python==4.9.0.80
Pillow>=10.0,<13

# ============================================================
# 2. VEKTORISIERUNG & GCODE — immer benötigt
# ============================================================

# vpype + vpype-gcode: SVG→GCode mit TOML-Profilen (GCodeFromSvgStep)
# Hinweis: vpype 1.15.x erfordert Python 3.11–3.13
vpype==1.15.0
vpype-gcode==0.13.0

# GRBL-Kommunikation (SendGcodeStep)
pygrbl_streamer @ git+https://github.com/offerrall/PyGrbl_Streamer.git
pyserial>=3.5

# ============================================================
# 3. NEURONALE KANTENERKENNUNG — für NN-basierte Stilisierer
# ============================================================

# HED, DexiNed, Lineart, Adaptive (via controlnet_aux)
controlnet-aux==0.0.10

# Stable Diffusion (ControlNet + Img2Img Style Transfer)
# Optional: nur benötigt für stylise_controlnet und stylise_img2img
diffusers
transformers
safetensors
accelerate  # Für effizientes Model Loading (sehr empfohlen)

# PyTorch: Plattform- und GPU-abhängig — pip installiert CPU-Build als Fallback.
# Für CUDA/MPS-Builds: https://pytorch.org/get-started/locally/
torch
torchvision
timm>=0.9,<2

# Informative Drawings (ONNX bevorzugt, PyTorch als Fallback)
huggingface-hub>=0.20,<2
onnxruntime>=1.16,<2

# ============================================================
# 4. ENTWICKLUNG & TESTS
# ============================================================

pytest>=9.0
