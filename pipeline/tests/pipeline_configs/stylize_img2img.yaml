# pipeline/tests/pipeline_configs/stylize_img2img.yaml
#
# Test pipeline with Image-to-Image style transfer using Stable Diffusion 2.1
# (lightweight model, no ControlNet)

name: "Img2Img Style Transfer"
description: "Image-to-Image style transfer via Stable Diffusion — applies artistic style without ControlNet conditioning."

steps:
  - step: load_image
    label: "Load Image"
    # The test runner provides the real image path via metadata['source_path']
    config: {}

  # Step 1: Apply img2img style transfer
  - step: stylise_img2img
    label: "Stylize via Img2Img"
    config:
      prompt: "oil painting, artistic style, detailed brushstrokes"
      negative_prompt: "blurry, watercolor, photorealistic"
      strength: 0.7 # How much to modify (0.0–1.0)
      num_inference_steps: 20
      guidance_scale: 7.5
      model_id: "stable-diffusion-v1-5/stable-diffusion-v1-5"
      hf_token_path: ".hf_token"
      enable_model_cpu_offload: false
      device: auto

  - step: save_image
    label: "Save Image"
    config:
      output_path: "output/img2img_image.png"
